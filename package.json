{
  "name": "customerhero-sdk",
  "version": "0.0.0",
  "private": true,
  "description": "Official JavaScript and React SDKs for CustomerHero",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/customerhero/customerhero-sdk.git"
  },
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/js && npm run build -w packages/react",
    "pretest": "npm run build -w packages/js",
    "test": "npm run test -w packages/js && npm run test -w packages/react",
    "pretypecheck": "npm run build -w packages/js",
    "typecheck": "npm run typecheck -w packages/js && npm run typecheck -w packages/react",
    "format": "prettier --write \"packages/**/*.{ts,tsx,json,md}\"",
    "format:check": "prettier --check \"packages/**/*.{ts,tsx,json,md}\"",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "npm run build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "prettier": "^3.3.3"
  },
  "engines": {
    "node": ">=18"
  }
}
